
import cv2
import numpy as np
import pandas as pd

def cal():
    # Example: Extract Average Pixel Intensity from a Sequence of Images
    image_paths = ["image1.jpg", "image2.jpg", "image3.jpg"]  # Sequential images
    # time_series_data = []

    # for idx, img_path in enumerate(image_paths):
    #     img = cv2.imread(img_path, cv2.IMREAD_GRAYSCALE)  # Read as grayscale
    #     avg_intensity = np.mean(img)  # Compute average pixel intensity
    #     time_series_data.append([idx, avg_intensity])  # Store as time series

    # # Convert to DataFrame
    # df = pd.DataFrame(time_series_data, columns=['Time', 'Avg_Pixel_Intensity'])
    # print(df)


def main():
    pass



